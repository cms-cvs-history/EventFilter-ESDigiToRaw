process DIGI2RAW = {

    untracked PSet maxEvents = {
	untracked int32 input = 2
    }

    source = PoolSource {
	untracked vstring fileNames = { 'file:PhysVal-DiTau-Pt20To420.root' }
    }

    module esDigiToRaw = ESDigiToRaw {
	string Label = "ecalPreshowerDigis"
        string InstanceES = ""
	untracked bool debugMode = true
	untracked int32 formatMajor = 4
	untracked int32 formatMinor = 0
	untracked FileInPath LookupTable = "EventFilter/ESDigiToRaw/data/ES_lookup_table.dat"
    }

    path p = { esDigiToRaw }

    module DIGIRAW = PoolOutputModule {
        untracked string fileName = "test.root"
    }

    endpath outpath = { DIGIRAW }

}
